<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.festadbolso">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- FirebaseUI Activity -->
        <activity
            android:name=".UI.FirebaseUIActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity
            android:name=".UI.MainActivity"
            android:exported="false" />

        <activity android:name=".UI.GameDetailsActivity">
            <!-- You can add additional attributes here, such as launch mode, theme, etc. -->
        </activity>

        <activity
            android:name=".UI.SpyActivity"
            android:theme="@style/Theme.AppCompat.Light" />

        <activity android:name=".UI.HostActivity">
            <!-- Optional: Any specific settings for HostActivity -->
        </activity>

        <activity android:name=".UI.JoinActivity">
            <!-- Optional: Any specific settings for JoinActivity -->
        </activity>

        <activity android:name=".UI.CharadesActivity">
            <!-- Optional: Any specific settings for JoinActivity -->
        </activity>

        <activity android:name=".UI.GameListActivity">
            <!-- Optional: Any specific settings for JoinActivity -->
        </activity>

        <activity android:name=".UI.TruthOrDareActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

    </application>
</manifest>